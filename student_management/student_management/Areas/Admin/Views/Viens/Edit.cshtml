@model student_management.Models.Vien

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Sửa thông tin Viện</h1>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <!-- left column -->
                <div class="col-md-6">
                    <!-- general form elements -->
                    <div class="card card-primary">
                        <div class="card-header">
                            <h3 class="card-title">Chỉnh sửa thông tin Viện</h3>
                        </div>

                        <!-- form start -->
                        <form asp-action="Edit" method="post">
                            <div class="card-body">
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                                <div class="form-group">
                                    <label asp-for="MaVien" class="control-label">Mã khoa</label>
                                    <!-- Mã khoa chỉ đọc (không được sửa) -->
                                    <input asp-for="MaVien" class="form-control" readonly />
                                    <span asp-validation-for="MaVien" class="text-danger"></span>
                                </div>

                                <div class="form-group">
                                    <label asp-for="TenVien" class="control-label">Tên khoa</label>
                                    <input asp-for="TenVien" class="form-control" placeholder="Nhập tên khoa..." />
                                    <span asp-validation-for="TenVien" class="text-danger"></span>
                                </div>
                                <div class="form-group">
                                    <label asp-for="MoTa" class="control-label">Tên khoa</label>
                                    <input asp-for="MoTa" class="form-control" placeholder="Nhập tên khoa..." />
                                    <span asp-validation-for="MoTa" class="text-danger"></span>
                                </div>
                            </div>

                            <div class="card-footer">
                                <input type="submit" value="Lưu thay đổi" class="btn btn-info" />
                                <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
                            </div>
                        </form>
                    </div>
                    <!-- /.card -->
                </div>
            </div>
            <!-- /.row -->
        </div><!-- /.container-fluid -->
    </section>
    <!-- /.content -->
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
